<%= form_for(structure_expense) do |f| %>
  <% if structure_expense.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(structure_expense.errors.count, "error") %> prohibited this structure_expense from being saved:</h2>

      <ul>
      <% structure_expense.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :program_production_id %>
    <%= f.text_field :program_production_id %>
  </div>

  <div class="field">
    <%= f.label :chart_of_account_id %>
    <%= f.text_field :chart_of_account_id %>
  </div>

  <div class="field">
    <%= f.label :total %>
    <%= f.text_field :total %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
