<%= form_for(machine) do |f| %>
  <% if machine.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(machine.errors.count, "error") %> prohibited this machine from being saved:</h2>

      <ul>
      <% machine.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :brand_id %>
    <%= f.text_field :brand_id %>
  </div>

  <div class="field">
    <%= f.label :model_id %>
    <%= f.text_field :model_id %>
  </div>

  <div class="field">
    <%= f.label :hp %>
    <%= f.text_field :hp %>
  </div>

  <div class="field">
    <%= f.label :consumption,"Coef. Consumo" %>
    <%= f.text_field :consumption %>
  </div>

  <div class="field">
    <%= f.label :price %>
    <%= f.text_field :price %>
  </div>

  <div class="field">
    <%= f.label :year_purchase %>
    <%= f.date_select :year_purchase %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
