$(document).ready(function() {
 
{
  var quant_errors = <%= @program_production.errors.count %>;
  if(quant_errors > 0)
      {
        console.log(quant_errors);
      }
      
      

          $('#form_program_production').validator().on('submit', function (e) {
            if (e.isDefaultPrevented()) {
              if($("#program_production_estimate_sale_id").val() == "" || $("#program_production_stock_end").val() == ""  || $("#program_production_stock_initial").val() == ""  || $("#program_production_variation_stock").val() == ""  || $("#program_production_program_production").val() =="" )
              { 
                
                alert_sarah("Debe cargar todos los campos", "danger");
              }     
                
            }
            else
            { 
                
                $('#form_program_production').on('ajax:success', function(event, xhr, status, error) {
                  // insert the failure message inside the "#account_settings" element
                  alert_sarah("El registro fue guardado con exito", "success");
                });
                
                              
              $("#form_program_production input" ).val("");
              $("#form_program_production select" ).val("");
            }
          });
     





});

});