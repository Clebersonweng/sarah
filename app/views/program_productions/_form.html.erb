<%= form_for(program_production) do |f| %>
  <% if program_production.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(program_production.errors.count, "error") %> prohibited this program_production from being saved:</h2>

      <ul>
      <% program_production.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
   <%= f.label :estimate_sale_id %>
   <%= select("program_production", "estimate_sale_id",options_for_select(EstimateSale.all.collect {|p| [ p.farming_plot.name, p.id, {"data-total-production"=>p.total_production} ] }))%> 
 </div>


 <div class="field">
    <%= f.label :stock_end %>
    <%= f.text_field :stock_end %>
  </div>

  <div class="field">
    <%= f.label :stock_initial %>
    <%= f.text_field :stock_initial %>
  </div>

 

  <div class="field">
    <%= f.label :variation_stock %>
    <%= f.text_field :variation_stock %>
  </div>

  <div class="field">
    <%= f.label :program_production %>
    <%= f.text_field :program_production %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
