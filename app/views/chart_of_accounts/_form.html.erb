<%= form_for(chart_of_account) do |f| %>
  <% if chart_of_account.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(chart_of_account.errors.count, "error") %> prohibited this chart_of_account from being saved:</h2>

      <ul>
      <% chart_of_account.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :code %>
    <%= f.text_field :code %>
  </div>

  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
