<%= form_for(mark_spending) do |f| %>
  <% if mark_spending.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(mark_spending.errors.count, "error") %> prohibited this mark_spending from being saved:</h2>

      <ul>
      <% mark_spending.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :estimate_sale_id %>
    <%= f.text_field :estimate_sale_id %>
  </div>

  <div class="field">
    <%= f.label :chart_of_account_id %>
    <%= f.text_field :chart_of_account_id %>
  </div>

  <div class="field">
    <%= f.label :total %>
    <%= f.text_field :total %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
